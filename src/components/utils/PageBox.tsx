import React from "react";
import { Box } from "@material-ui/core";

const PageBox = () => {
  return (
    <Box
      style={{
        marginTop: "30px",
        maxWidth: "600px",
        flexDirection: "column",
        alignItems: "center",
        display: "flex",
      }}
    ></Box>
  );
};

export default PageBox;
